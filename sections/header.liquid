{% comment %}
  Custom Header section for NüHome theme
  Features: Announcement bar, support box, logo, navigation, search, account, cart
  Combines COWSAR structure with NüHome modern aesthetic
{% endcomment %}

<!-- Top Announcement Bar -->
<div class="announcement-bar" data-announcement-bar>
  <div class="container">
    <div class="announcement-bar__content">
      <i
        data-lucide="package"
        class="announcement-bar__icon"
        aria-hidden="true"></i>
      <span class="announcement-bar__text">{{ section.settings.announcement_text }}</span>
      <i
        data-lucide="truck"
        class="announcement-bar__icon"
        aria-hidden="true"></i>
      <button
        class="announcement-bar__close"
        type="button"
        aria-label="Close"
        data-announcement-close>×</button>
    </div>
  </div>
</div>

<!-- Main Header -->
<header
  class="header"
  data-section-id="{{ section.id }}"
  data-section-type="header">
  <div class="container">
    <div class="header__content">
      <!-- Left: Support Box -->
      <div class="header__support">
        <div class="support-box">
          <i
            data-lucide="headphones"
            class="support-box__icon"
            aria-hidden="true"></i>
          <div class="support-box__content">
            <div class="support-box__label">Customer Support</div>
            <button class="support-box__claim-btn" data-claim-modal-toggle>
              Submit Claim
            </button>
          </div>
        </div>
      </div>

      <!-- Center: Logo -->
      <div class="header__logo">
        {% if section.settings.logo != blank %}
          <a href="/" class="header__logo-link">
            <img
              src="{{ section.settings.logo | image_url: width: section.settings.logo_width }}"
              alt="{{ shop.name }}"
              width="{{ section.settings.logo_width }}"
              height="{{ section.settings.logo_width | divided_by: section.settings.logo.aspect_ratio | round }}"
              loading="eager">
          </a>
        {% else %}
          <a href="/" class="header__logo-text">
            NüH❄️me
          </a>
        {% endif %}
      </div>

      <!-- Right: Header Actions -->
      <div class="header__actions">
        <!-- Search -->
        <button
          class="header__action-btn"
          data-search-toggle
          aria-label="Toggle search"
          aria-expanded="false">
          <i
            data-lucide="search"
            class="header__action-icon"
            aria-hidden="true"></i>
        </button>

        <!-- Account -->
        <a
          href="/account"
          class="header__action-btn"
          aria-label="Account">
          <i
            data-lucide="user"
            class="header__action-icon"
            aria-hidden="true"></i>
        </a>

        <!-- Cart -->
        <a
          href="/cart"
          class="header__action-btn header__cart-btn"
          aria-label="Cart">
          <i
            data-lucide="shopping-cart"
            class="header__action-icon"
            aria-hidden="true"></i>
          <span class="header__cart-count" data-cart-count>
            {% if cart.item_count > 0 %}
              {{ cart.item_count }}
            {% endif %}
          </span>
        </a>
      </div>

      <!-- Mobile Menu Toggle -->
      <button
        class="header__mobile-toggle"
        data-mobile-menu-toggle
        aria-label="Toggle mobile menu"
        aria-expanded="false">
        <span class="header__mobile-toggle-line"></span>
        <span class="header__mobile-toggle-line"></span>
        <span class="header__mobile-toggle-line"></span>
      </button>
    </div>

    <!-- Navigation Menu -->
    <nav class="header__nav" role="navigation">
      <ul class="header__nav-list">
        <!-- ICE MAKERS -->
        <li class="header__nav-item header__nav-item--has-dropdown">
          <a href="/collections/ice-makers" class="header__nav-link">
            ICE MAKERS
            <span class="header__nav-arrow">▼</span>
          </a>
          <div class="header__nav-mega-dropdown">
            <div class="header__nav-mega-content">
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Nugget Ice Makers</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-nugget-ice-maker-countertop">COWSAR Nugget Ice Maker</a>
                  </li>
                  <li>
                    <a href="/products/cowsar-nugget-ice-maker-dispenser">COWSAR Nugget Dispenser</a>
                  </li>
                  <li>
                    <a href="/products/euhomy-nugget-ice-makers">Euhomy Nugget Ice Makers</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Portable Ice Makers</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-portable-ice-makers">COWSAR Portable Ice</a>
                  </li>
                  <li>
                    <a href="/products/cowsar-bullet-ice-maker">COWSAR Bullet Ice Maker</a>
                  </li>
                  <li>
                    <a href="/products/euhomy-ice-maker-machine">EUHOMY Ice Maker</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Commercial Ice Makers</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-commercial-ice-maker">COWSAR Commercial</a>
                  </li>
                  <li>
                    <a href="/products/euhomy-commercial-ice-maker">EUHOMY Commercial</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-featured">
                <div class="header__nav-mega-promo">
                  <span class="header__nav-mega-promo-badge">Best Seller</span>
                  <h5>COWSAR Nugget Ice Maker</h5>
                  <p>Soft, chewable ice in minutes</p>
                  <a href="/products/cowsar-nugget-ice-maker-countertop" class="btn btn-sm">Shop Now</a>
                </div>
              </div>
            </div>
          </div>
        </li>

        <!-- COFFEE MAKERS -->
        <li class="header__nav-item header__nav-item--has-dropdown">
          <a href="/collections/coffee-makers" class="header__nav-link">
            COFFEE MAKERS
            <span class="header__nav-arrow">▼</span>
          </a>
          <div class="header__nav-mega-dropdown">
            <div class="header__nav-mega-content">
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Drip Coffee Makers</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-drip-coffee-maker">COWSAR Drip Coffee</a>
                  </li>
                  <li>
                    <a href="/products/cowsar-10-cup-coffee-maker">COWSAR 10-Cup Coffee</a>
                  </li>
                  <li>
                    <a href="/products/cowsar-5-cup-coffee">Cowsar 5-Cup</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Espresso Machines</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-espresso-machine">COWSAR Espresso</a>
                  </li>
                  <li>
                    <a href="/products/cowsar-espresso-cappuccino">COWSAR Cappuccino</a>
                  </li>
                  <li>
                    <a href="/products/cowsar-espresso-20bar">COWSAR 20 Bar</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Coffee Accessories</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/collections/coffee-filters">Coffee Filters</a>
                  </li>
                  <li>
                    <a href="/collections/coffee-grinders">Coffee Grinders</a>
                  </li>
                  <li>
                    <a href="/collections/coffee-storage">Coffee Storage</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-featured">
                <div class="header__nav-mega-promo">
                  <span class="header__nav-mega-promo-badge">Premium</span>
                  <h5>Espresso Collection</h5>
                  <p>Barista-quality coffee at home</p>
                  <a href="/collections/espresso-machines" class="btn btn-sm">Shop Coffee</a>
                </div>
              </div>
            </div>
          </div>
        </li>

        <!-- COOLERS -->
        <li class="header__nav-item header__nav-item--has-dropdown">
          <a href="/collections/coolers" class="header__nav-link">
            COOLERS
            <span class="header__nav-arrow">▼</span>
          </a>
          <div class="header__nav-mega-dropdown">
            <div class="header__nav-mega-content">
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Portable Coolers</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-portable-cooler-12v">COWSAR 12V Cooler</a>
                  </li>
                  <li>
                    <a href="/products/euhomy-12v-refrigerator">EUHOMY 12V Refrigerator</a>
                  </li>
                  <li>
                    <a href="/products/euhomy-car-refrigerator">EUHOMY Car Fridge</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Mini Fridges</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/products/cowsar-mini-fridge">Cowsar Mini Fridge</a>
                  </li>
                  <li>
                    <a href="/products/euhomy-mini-fridge">EUHOMY Mini Fridge</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-column">
                <h4 class="header__nav-mega-title">Wine & Beverage</h4>
                <ul class="header__nav-mega-list">
                  <li>
                    <a href="/collections/wine-coolers">Wine Coolers</a>
                  </li>
                  <li>
                    <a href="/collections/beverage-centers">Beverage Centers</a>
                  </li>
                  <li>
                    <a href="/collections/water-dispensers">Water Dispensers</a>
                  </li>
                </ul>
              </div>
              <div class="header__nav-mega-featured">
                <div class="header__nav-mega-promo">
                  <span class="header__nav-mega-promo-badge">New</span>
                  <h5>12V Car Refrigerator</h5>
                  <p>Perfect for road trips & camping</p>
                  <a href="/collections/portable-refrigerators" class="btn btn-sm">Explore</a>
                </div>
              </div>
            </div>
          </div>
        </li>

        <!-- ABOUT US -->
        <li class="header__nav-item">
          <a href="/pages/about-us" class="header__nav-link">
            ABOUT US
          </a>
        </li>

        <!-- BEST SELLERS -->
        <li class="header__nav-item">
          <a href="/collections/bestsellers" class="header__nav-link">
            BEST SELLERS
          </a>
        </li>

        <!-- SUPPORT -->
        <li class="header__nav-item">
          <a href="/pages/customer-care" class="header__nav-link">
            SUPPORT
          </a>
        </li>
      </ul>
    </nav>

    {% if section.settings.show_tagline %}
      <div class="header__tagline" role="note">
        {{ section.settings.tagline_text | default: "We create stylish, affordable home makers that simplify your life and elevate your daily rituals." }}
      </div>
    {% endif %}

    <!-- Search Form -->
    <div class="header__search" data-search-form>
      <form
        action="/search"
        method="get"
        class="header__search-form">
        <input
          type="search"
          name="q"
          placeholder="Search products..."
          class="header__search-input"
          aria-label="Search">
        <button
          type="submit"
          class="header__search-btn"
          aria-label="Search">
          <span class="header__action-icon">🔍</span>
        </button>
      </form>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="header__mobile-menu" data-mobile-menu>
    <div class="header__mobile-menu-content">
      <div class="header__mobile-header">
        <div class="header__mobile-logo">
          <a href="/" class="header__mobile-logo-link">
            {% if section.settings.logo != blank %}
              <img
                src="{{ section.settings.logo | image_url: width: 120 }}"
                alt="{{ shop.name }}"
                width="120"
                height="40">
            {% else %}
              <span class="header__mobile-logo-text">NüH❄️me</span>
            {% endif %}
          </a>
        </div>
        <button
          class="header__mobile-close"
          data-mobile-menu-close
          aria-label="Close mobile menu">
          <i data-lucide="x" class="header__mobile-close-icon"></i>
        </button>
      </div>

      <nav class="header__mobile-nav">
        <ul class="header__mobile-nav-list">
          <li class="header__mobile-nav-item">
            <a href="/" class="header__mobile-nav-link">Home</a>
          </li>

          <li class="header__mobile-nav-item header__mobile-nav-item--has-dropdown">
            <button class="header__mobile-nav-link header__mobile-nav-toggle" data-mobile-dropdown-toggle>
              ICE MAKERS
              <i data-lucide="chevron-down" class="header__mobile-nav-arrow"></i>
            </button>
            <ul class="header__mobile-dropdown" data-mobile-dropdown>
              <li>
                <a href="/collections/ice-makers">All Ice Makers</a>
              </li>
              <li>
                <a href="/products/cowsar-nugget-ice-maker-countertop">COWSAR Nugget Ice Maker</a>
              </li>
              <li>
                <a href="/products/cowsar-nugget-ice-maker-dispenser">COWSAR Nugget Dispenser</a>
              </li>
              <li>
                <a href="/products/euhomy-nugget-ice-makers">Euhomy Nugget Ice Makers</a>
              </li>
              <li>
                <a href="/products/cowsar-portable-ice-makers">COWSAR Portable Ice</a>
              </li>
              <li>
                <a href="/products/cowsar-bullet-ice-maker">COWSAR Bullet Ice Maker</a>
              </li>
            </ul>
          </li>

          <li class="header__mobile-nav-item header__mobile-nav-item--has-dropdown">
            <button class="header__mobile-nav-link header__mobile-nav-toggle" data-mobile-dropdown-toggle>
              COFFEE MAKERS
              <i data-lucide="chevron-down" class="header__mobile-nav-arrow"></i>
            </button>
            <ul class="header__mobile-dropdown" data-mobile-dropdown>
              <li>
                <a href="/collections/coffee-makers">All Coffee Makers</a>
              </li>
              <li>
                <a href="/products/cowsar-drip-coffee-maker">COWSAR Drip Coffee</a>
              </li>
              <li>
                <a href="/products/cowsar-10-cup-coffee-maker">COWSAR 10-Cup Coffee</a>
              </li>
              <li>
                <a href="/products/cowsar-espresso-machine">COWSAR Espresso</a>
              </li>
              <li>
                <a href="/products/cowsar-espresso-cappuccino">COWSAR Cappuccino</a>
              </li>
            </ul>
          </li>

          <li class="header__mobile-nav-item header__mobile-nav-item--has-dropdown">
            <button class="header__mobile-nav-link header__mobile-nav-toggle" data-mobile-dropdown-toggle>
              COOLERS
              <i data-lucide="chevron-down" class="header__mobile-nav-arrow"></i>
            </button>
            <ul class="header__mobile-dropdown" data-mobile-dropdown>
              <li>
                <a href="/collections/coolers">All Coolers</a>
              </li>
              <li>
                <a href="/products/cowsar-portable-cooler-12v">COWSAR 12V Cooler</a>
              </li>
              <li>
                <a href="/products/euhomy-12v-refrigerator">EUHOMY 12V Refrigerator</a>
              </li>
              <li>
                <a href="/products/euhomy-car-refrigerator">EUHOMY Car Fridge</a>
              </li>
              <li>
                <a href="/products/cowsar-mini-fridge">Cowsar Mini Fridge</a>
              </li>
            </ul>
          </li>

          <li class="header__mobile-nav-item">
            <a href="/collections/bestsellers" class="header__mobile-nav-link">Best Sellers</a>
          </li>

          <li class="header__mobile-nav-item">
            <a href="/pages/about-us" class="header__mobile-nav-link">About Us</a>
          </li>

          <li class="header__mobile-nav-item">
            <a href="/pages/customer-care" class="header__mobile-nav-link">Support</a>
          </li>
        </ul>
      </nav>

      <div class="header__mobile-actions">
        <div class="header__mobile-support">
          <i data-lucide="headphones" class="header__mobile-support-icon"></i>
          <div class="header__mobile-support-content">
            <div class="header__mobile-support-label">Customer Support</div>
            <button class="header__mobile-support-btn" data-claim-modal-toggle>
              Submit Claim
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Claim Modal -->
  <div class="claim-modal" data-claim-modal>
    <div class="claim-modal__overlay" data-claim-modal-close></div>
    <div class="claim-modal__content">
      <div class="claim-modal__header">
        <h3 class="claim-modal__title">Submit Customer Claim</h3>
        <button
          class="claim-modal__close"
          data-claim-modal-close
          aria-label="Close">
          <i data-lucide="x" class="claim-modal__close-icon"></i>
        </button>
      </div>

      <form
        class="claim-form"
        action="/contact"
        method="post"
        data-claim-form>
        <input
          type="hidden"
          name="form_type"
          value="contact">
        <input
          type="hidden"
          name="utf8"
          value="✓">
        <input
          type="hidden"
          name="contact[email]"
          value="bmaxsocialmedia20@gmail.com">
        <input
          type="hidden"
          name="contact[subject]"
          value="Customer Claim - NüHome">

        <div class="claim-form__field">
          <label for="claim-name" class="claim-form__label">Full Name *</label>
          <input
            type="text"
            id="claim-name"
            name="contact[name]"
            class="claim-form__input"
            required
            placeholder="Enter your full name">
        </div>

        <div class="claim-form__field">
          <label for="claim-email" class="claim-form__label">Email Address *</label>
          <input
            type="email"
            id="claim-email"
            name="contact[email_customer]"
            class="claim-form__input"
            required
            placeholder="Enter your email address">
        </div>

        <div class="claim-form__field">
          <label for="claim-order" class="claim-form__label">Order Number (if applicable)</label>
          <input
            type="text"
            id="claim-order"
            name="contact[order_number]"
            class="claim-form__input"
            placeholder="Enter order number">
        </div>

        <div class="claim-form__field">
          <label for="claim-product" class="claim-form__label">Product Name</label>
          <input
            type="text"
            id="claim-product"
            name="contact[product_name]"
            class="claim-form__input"
            placeholder="Enter product name">
        </div>

        <div class="claim-form__field">
          <label for="claim-type" class="claim-form__label">Claim Type *</label>
          <select
            id="claim-type"
            name="contact[claim_type]"
            class="claim-form__select"
            required>
            <option value="">Select claim type</option>
            <option value="defective_product">Defective Product</option>
            <option value="wrong_item">Wrong Item Received</option>
            <option value="damaged_delivery">Damaged During Delivery</option>
            <option value="missing_parts">Missing Parts</option>
            <option value="warranty_claim">Warranty Claim</option>
            <option value="refund_request">Refund Request</option>
            <option value="other">Other</option>
          </select>
        </div>

        <div class="claim-form__field">
          <label for="claim-description" class="claim-form__label">Description *</label>
          <textarea
            id="claim-description"
            name="contact[body]"
            class="claim-form__textarea"
            rows="4"
            required
            placeholder="Please describe your claim in detail..."></textarea>
        </div>

        <div class="claim-form__actions">
          <button
            type="button"
            class="claim-form__btn claim-form__btn--secondary"
            data-claim-modal-close>
            Cancel
          </button>
          <button type="submit" class="claim-form__btn claim-form__btn--primary">
            <span class="claim-form__btn-text">Submit Claim</span>
            <span class="claim-form__btn-loading" style="display: none;">
              <i data-lucide="loader-2" class="claim-form__loading-icon"></i>
              Sending...
            </span>
          </button>
        </div>
      </form>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Sticky Header Functionality
      const header = document.querySelector('.header');
      let lastScrollTop = 0;
      
      window.addEventListener('scroll', function() {
        const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
        
        if (scrollTop > 50) {
          header.classList.add('header--scrolled');
        } else {
          header.classList.remove('header--scrolled');
        }
        
        lastScrollTop = scrollTop;
      });

      // Mobile Menu Functionality
      const mobileMenuToggle = document.querySelector('[data-mobile-menu-toggle]');
      const mobileMenu = document.querySelector('[data-mobile-menu]');
      const mobileMenuClose = document.querySelector('[data-mobile-menu-close]');
      const mobileDropdownToggles = document.querySelectorAll('[data-mobile-dropdown-toggle]');
      
      // Toggle mobile menu
      if (mobileMenuToggle && mobileMenu) {
        mobileMenuToggle.addEventListener('click', function() {
          mobileMenu.classList.toggle('active');
          mobileMenuToggle.classList.toggle('active');
          mobileMenuToggle.setAttribute('aria-expanded', 
            mobileMenu.classList.contains('active').toString());
          
          // Prevent body scroll when menu is open
          if (mobileMenu.classList.contains('active')) {
            document.body.style.overflow = 'hidden';
          } else {
            document.body.style.overflow = '';
          }
        });
      }
      
      // Close mobile menu
      if (mobileMenuClose && mobileMenu) {
        mobileMenuClose.addEventListener('click', function() {
          mobileMenu.classList.remove('active');
          mobileMenuToggle.classList.remove('active');
          mobileMenuToggle.setAttribute('aria-expanded', 'false');
          document.body.style.overflow = '';
        });
      }
      
      // Mobile dropdown toggles
      mobileDropdownToggles.forEach(function(toggle) {
        toggle.addEventListener('click', function() {
          const navItem = this.closest('.header__mobile-nav-item--has-dropdown');
          navItem.classList.toggle('active');
        });
      });
      
      // Close mobile menu when clicking outside
      document.addEventListener('click', function(e) {
        if (mobileMenu && mobileMenu.classList.contains('active')) {
          if (!mobileMenu.contains(e.target) && !mobileMenuToggle.contains(e.target)) {
            mobileMenu.classList.remove('active');
            mobileMenuToggle.classList.remove('active');
            mobileMenuToggle.setAttribute('aria-expanded', 'false');
            document.body.style.overflow = '';
          }
        }
      });
      
      // Close mobile menu when clicking on navigation links
      const mobileNavLinks = mobileMenu.querySelectorAll('a[href]');
      mobileNavLinks.forEach(function(link) {
        link.addEventListener('click', function() {
          mobileMenu.classList.remove('active');
          mobileMenuToggle.classList.remove('active');
          mobileMenuToggle.setAttribute('aria-expanded', 'false');
          document.body.style.overflow = '';
        });
      });

      // Cart Count Update Functionality
      function updateCartCount() {
        const cartCountElements = document.querySelectorAll('[data-cart-count]');
        
        // Fetch current cart count from Shopify
        fetch('/cart.js')
          .then(response => response.json())
          .then(cart => {
            cartCountElements.forEach(function(element) {
              if (cart.item_count > 0) {
                element.textContent = cart.item_count;
                element.style.display = 'flex';
              } else {
                element.style.display = 'none';
              }
            });
          })
          .catch(error => {
            console.error('Error fetching cart count:', error);
          });
      }
      
      // Update cart count on page load
      updateCartCount();
      
      // Listen for cart changes (Shopify's cart API)
      document.addEventListener('cart:updated', updateCartCount);
      
      // Also update on any form submission that might affect cart
      document.addEventListener('submit', function(e) {
        if (e.target.matches('form[action*="/cart/add"]')) {
          setTimeout(updateCartCount, 1000);
        }
      });

      // Claim Modal Functionality
      const claimModal = document.querySelector('[data-claim-modal]');
      const claimModalToggle = document.querySelector('[data-claim-modal-toggle]');
      const claimModalClose = document.querySelectorAll('[data-claim-modal-close]');
      const claimForm = document.querySelector('[data-claim-form]');
      
      // Open modal
      if (claimModalToggle) {
        claimModalToggle.addEventListener('click', function(e) {
          e.preventDefault();
          claimModal.classList.add('active');
          document.body.style.overflow = 'hidden';
        });
      }
      
      // Close modal
      claimModalClose.forEach(function(closeBtn) {
        closeBtn.addEventListener('click', function() {
          claimModal.classList.remove('active');
          document.body.style.overflow = '';
        });
      });
      
      // Close modal on escape key
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && claimModal.classList.contains('active')) {
          claimModal.classList.remove('active');
          document.body.style.overflow = '';
        }
      });
      
      // Handle form submission
      if (claimForm) {
        claimForm.addEventListener('submit', function(e) {
          e.preventDefault();
          
          const submitBtn = claimForm.querySelector('.claim-form__btn--primary');
          const btnText = submitBtn.querySelector('.claim-form__btn-text');
          const btnLoading = submitBtn.querySelector('.claim-form__btn-loading');
          
          // Show loading state
          submitBtn.disabled = true;
          btnText.style.display = 'none';
          btnLoading.style.display = 'flex';
          
          // Get form data
          const formData = new FormData(claimForm);
          
          // Submit form using fetch
          fetch('/contact', {
            method: 'POST',
            body: formData,
            headers: {
              'Accept': 'application/json',
            }
          })
          .then(response => {
            if (response.ok) {
              // Show success message
              showMessage('Thank you! Your claim has been submitted successfully. We will get back to you soon.', 'success');
              claimForm.reset();
            } else {
              throw new Error('Submission failed');
            }
          })
          .catch(error => {
            console.error('Error:', error);
            showMessage('Sorry, there was an error submitting your claim. Please try again or contact us directly.', 'error');
          })
          .finally(() => {
            // Reset button state
            submitBtn.disabled = false;
            btnText.style.display = 'inline';
            btnLoading.style.display = 'none';
          });
        });
      }
      
      // Function to show messages
      function showMessage(message, type) {
        // Remove existing messages
        const existingMessage = claimForm.querySelector('.claim-form__message');
        if (existingMessage) {
          existingMessage.remove();
        }
        
        // Create new message
        const messageDiv = document.createElement('div');
        messageDiv.className = `claim-form__message claim-form__message--${type}`;
        messageDiv.textContent = message;
        
        // Insert at the top of the form
        claimForm.insertBefore(messageDiv, claimForm.firstChild);
        
        // Auto-remove after 5 seconds
        setTimeout(() => {
          if (messageDiv.parentNode) {
            messageDiv.remove();
          }
        }, 5000);
      }
    });
  </script>
</header>

{% schema %}
  {
    "name": "Header",
    "settings": [
      {
        "type": "image_picker",
        "id": "logo",
        "label": "Logo"
      },
      {
        "type": "range",
        "id": "logo_width",
        "min": 50,
        "max": 300,
        "step": 10,
        "default": 180,
        "unit": "px",
        "label": "Logo width"
      },
      {
        "type": "link_list",
        "id": "menu",
        "label": "Menu",
        "default": "main-menu"
      },
      {
        "type": "header",
        "content": "Brand Tagline"
      }, {
        "type": "checkbox",
        "id": "show_tagline",
        "label": "Show tagline under navigation",
        "default": true
      }, {
        "type": "textarea",
        "id": "tagline_text",
        "label": "Tagline text",
        "default": "We create stylish, affordable home makers that simplify your life and elevate your daily rituals."
      }, {
        "type": "text",
        "id": "support_phone",
        "label": "Support Phone Number",
        "default": "+1-888-555-1212"
      }, {
        "type": "text",
        "id": "announcement_text",
        "label": "Announcement Text",
        "default": "Shipping from US Inventory — Delivery in 2–5 Days"
      }
    ]
  }
{% endschema %}

<style>
  /* Announcement Bar */
  .announcement-bar {
    background: #FFFFFD;
    padding: 8px 0;
    border-bottom: 1px solid rgba(74, 144, 226, 0.1);
  }

  .announcement-bar__content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    font-size: 14px;
    font-weight: 600;
    color: #222;
    min-height: 40px;
    padding: 0 48px;
    position: relative;
  }

  .announcement-bar__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .announcement-bar__icon svg {
    width: 16px;
    height: 16px;
  }

  .announcement-bar__text {
    text-align: center;
  }

  .announcement-bar__close {
    position: absolute;
    right: 16px;
    top: 50%;
    transform: translateY(-50%);
    background: transparent;
    border: none;
    font-size: 18px;
    line-height: 1;
    cursor: pointer;
    color: #2b6cb0;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: background-color 0.2s ease;
  }

  .announcement-bar__close:hover {
    background-color: rgba(43, 108, 176, 0.1);
  }

  /* Main Header */
  .header {
    background: #FFFFFD;
    border-bottom: 1px solid #f4f4f4;
    position: sticky;
    top: 0;
    z-index: var(--z-sticky);
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 253, 0.98);
  }

  .header.header--scrolled {
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    background: rgba(255, 255, 253, 0.95);
  }

  .header__content {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 24px;
    padding: 20px 0;
  }

  /* Support Box */
  .header__support {
    justify-self: start;
  }

  .support-box {
    display: flex;
    align-items: center;
    gap: 12px;
    background: linear-gradient(135deg, #F8FBFF 0%, #EBF4FF 100%);
    padding: 12px 16px;
    border-radius: 12px;
    border: 1px solid rgba(74, 144, 226, 0.2);
    box-shadow: 0 2px 8px rgba(74, 144, 226, 0.1);
  }

  .support-box__icon {
    font-size: 20px;
  }

  .support-box__content {
    display: flex;
    flex-direction: column;
  }

  .support-box__label {
    font-size: 12px;
    font-weight: 600;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .support-box__phone {
    font-size: 14px;
    font-weight: 700;
    color: #222;
  }

  .support-box__claim-btn {
    background: linear-gradient(135deg, #4A90E2 0%, #87CEEB 100%);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 6px 12px;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 4px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: 0 2px 6px rgba(74, 144, 226, 0.3);
  }

  .support-box__claim-btn:hover {
    background: linear-gradient(135deg, #3A7BD5 0%, #6BB6D6 100%);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(74, 144, 226, 0.4);
  }

  /* Logo */
  .header__logo {
    justify-self: center;
  }

  .header__logo-text {
    font-family: 'Inter'
    , -apple-system
    , BlinkMacSystemFont
    , 'Segoe UI'
    , Roboto
    , sans-serif;
    font-size: 28px;
    font-weight: 700;
    color: #4A90E2;
    text-decoration: none;
    letter-spacing: -0.5px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #F8FBFF 0%, #EBF4FF 100%);
    padding: 12px 20px;
    border-radius: 12px;
    border: 1px solid rgba(74, 144, 226, 0.2);
    box-shadow: 0 2px 8px rgba(74, 144, 226, 0.1);
    transition: all 0.3s ease;
  }

  .header__logo-text:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(74, 144, 226, 0.15);
    border-color: rgba(74, 144, 226, 0.3);
  }

  /* Snowflake animation for the logo */
  @keyframes snowflake-glow {
    0,
    100% {
      text-shadow: 0 2px 4px rgba(74, 144, 226, 0.2);
    }
    50% {
      text-shadow: 0 4px 12px rgba(74, 144, 226, 0.4)
      , 0 0 20px rgba(135, 206, 235, 0.3);
    }
  }

  /* Header Actions */
  .header__actions {
    display: flex;
    align-items: center;
    gap: 16px;
    justify-self: end;
  }

  .header__action-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border: none;
    background: transparent;
    color: #222;
    cursor: pointer;
    transition: all 0.2s ease;
    border-radius: 12px;
    position: relative;
  }

  .header__action-btn:hover {
    background: linear-gradient(135deg, #F0F8FF 0%, #E6F3FF 100%);
    color: #4A90E2;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(74, 144, 226, 0.2);
  }

  .header__action-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }

  .header__action-icon svg {
    width: 20px;
    height: 20px;
  }

  .header__cart-count {
    position: absolute;
    top: -8px;
    right: -8px;
    background: linear-gradient(135deg, #FF6B6B 0%, #FF8E8E 100%);
    color: white;
    font-size: 11px;
    font-weight: 700;
    min-width: 20px;
    height: 20px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 8px rgba(255, 107, 107, 0.4);
    padding: 0 6px;
    animation: cartCountPulse 0.6s ease-out;
  }

  @keyframes cartCountPulse {
    0% {
      transform: scale(0.8);
      opacity: 0.8;
    }
    50% {
      transform: scale(1.2);
      opacity: 1;
    }
    100% {
      transform: scale(1);
      opacity: 1;
    }
  }

  .cart-count-updated {
    animation: cartCountPulse 0.6s ease-out;
  }

  /* Navigation */
  .header__nav {
    border-top: 1px solid #f4f4f4;
    padding: 16px 0;
  }

  .header__tagline {
    text-align: center;
    color: #4A5568;
    font-size: 13px;
    font-weight: 500;
    margin: 8px 0 0;
    padding-bottom: 8px;
  }

  .header__nav-list {
    display: flex;
    list-style: none;
    gap: 32px;
    justify-content: center;
    margin: 0;
    padding: 0;
  }

  .header__nav-link {
    color: #222;
    text-decoration: none;
    font-weight: 500;
    font-size: 16px;
    transition: color 0.2s ease;
    padding: 8px 0;
    position: relative;
  }

  .header__nav-link:hover {
    color: #4A90E2;
  }

  /* Navigation Arrows */

  .header__nav-arrow {
    margin-left: 8px;
    font-size: 12px;
    transition: transform 0.3s ease;
  }

  .header__nav-item--has-dropdown:hover .header__nav-arrow {
    transform: rotate(180deg);
  }

  /* Sale Badge */
  .header__nav-sale-badge {
    background: linear-gradient(135deg, #FF6B6B 0%, #FF8E8E 100%);
    color: white;
    font-size: 10px;
    font-weight: 700;
    padding: 2px 6px;
    border-radius: 8px;
    margin-left: 8px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    animation: pulse-sale 2s ease-in-out infinite;
  }

  @keyframes pulse-sale {
    0,
    100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }

  /* Mega Dropdown */
  .header__nav-mega-dropdown {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    background: #FFFFFD;
    border: 1px solid rgba(74, 144, 226, 0.1);
    border-radius: 16px;
    box-shadow: 0 20px 60px rgba(74, 144, 226, 0.15);
    padding: 40px;
    width: 900px;
    max-width: 90vw;
    opacity: 0;
    visibility: hidden;
    transform: translateX(-50%) translateY(20px);
    transition: all 0.3s ease;
    z-index: 1000;
    backdrop-filter: blur(20px);
    background: rgba(255, 255, 253, 0.98);
  }

  .header__nav-item--has-dropdown:hover .header__nav-mega-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(0);
  }

  .header__nav-mega-content {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr 280px;
    gap: 40px;
  }

  .header__nav-mega-column {
    display: flex;
    flex-direction: column;
  }

  .header__nav-mega-title {
    font-size: 16px;
    font-weight: 700;
    color: #222;
    margin-bottom: 16px;
    padding-bottom: 8px;
    border-bottom: 2px solid rgba(74, 144, 226, 0.1);
    position: relative;
  }

  .header__nav-mega-title::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 30px;
    height: 2px;
    background: linear-gradient(90deg, #4A90E2 0%, #87CEEB 100%);
  }

  .header__nav-mega-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .header__nav-mega-list a {
    color: #666;
    text-decoration: none;
    font-size: 14px;
    font-weight: 500;
    padding: 6px 0;
    transition: all 0.3s ease;
    position: relative;
  }

  .header__nav-mega-list a::before {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 0;
    height: 1px;
    background: linear-gradient(90deg, #4A90E2 0%, #87CEEB 100%);
    transition: width 0.3s ease;
  }

  .header__nav-mega-list a:hover {
    color: #4A90E2;
    transform: translateX(5px);
  }

  .header__nav-mega-list a:hover::before {
    width: 100%;
  }

  /* Featured Promo in Mega Menu */
  .header__nav-mega-featured {
    background: linear-gradient(135deg, #F0F8FF 0%, #E6F3FF 100%);
    border-radius: 16px;
    padding: 24px;
    border: 1px solid rgba(74, 144, 226, 0.2);
    position: relative;
    overflow: hidden;
  }

  .header__nav-mega-featured::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    animation: shimmer-mega 3s infinite;
  }

  @keyframes shimmer-mega {
    0% {
      left: -100%;
    }
    100% {
      left: 100%;
    }
  }

  .header__nav-mega-promo {
    position: relative;
    z-index: 1;
  }

  .header__nav-mega-promo-badge {
    display: inline-block;
    background: linear-gradient(135deg, #4A90E2 0%, #87CEEB 100%);
    color: white;
    font-size: 10px;
    font-weight: 700;
    padding: 4px 8px;
    border-radius: 8px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 12px;
  }

  .header__nav-mega-promo h5 {
    font-size: 16px;
    font-weight: 700;
    color: #222;
    margin: 0 0 8px;
  }

  .header__nav-mega-promo p {
    font-size: 14px;
    color: #666;
    margin: 0 0 16px;
    line-height: 1.4;
  }

  .header__nav-mega-promo .btn {
    font-size: 12px;
    padding: 8px 16px;
    font-weight: 600;
    border-radius: 8px;
  }

  .header__nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, #4A90E2 0%, #87CEEB 100%);
    transition: width 0.2s ease;
  }

  .header__nav-link:hover::after {
    width: 100%;
  }

  /* Search Form */
  .header__search {
    display: none;
    padding: 16px 0;
    border-top: 1px solid #f4f4f4;
  }

  .header__search.active {
    display: block;
  }

  .header__search-form {
    display: flex;
    gap: 12px;
    max-width: 400px;
    margin: 0 auto;
  }

  .header__search-input {
    flex: 1;
    padding: 12px 16px;
    border: 2px solid #f4f4f4;
    border-radius: 12px;
    font-size: 16px;
    transition: border-color 0.2s ease;
  }

  .header__search-input:focus {
    outline: none;
    border-color: #4A90E2;
    box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.1);
  }

  .header__search-btn {
    padding: 12px 16px;
    background: linear-gradient(135deg, #4A90E2 0%, #87CEEB 100%);
    color: white;
    border: none;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
    box-shadow: 0 2px 6px rgba(74, 144, 226, 0.3);
  }

  .header__search-btn:hover {
    background: linear-gradient(135deg, #3A7BD5 0%, #6BB6D6 100%);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(74, 144, 226, 0.4);
  }

  /* Mobile Menu Toggle */
  .header__mobile-toggle {
    display: none;
    flex-direction: column;
    gap: 4px;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 8px;
    border-radius: 8px;
    transition: all 0.2s ease;
  }

  .header__mobile-toggle:hover {
    background: rgba(74, 144, 226, 0.1);
  }

  .header__mobile-toggle-line {
    width: 24px;
    height: 2px;
    background: #222;
    transition: all 0.3s ease;
    transform-origin: center;
  }

  .header__mobile-toggle.active .header__mobile-toggle-line:nth-child(1) {
    transform: rotate(45deg) translate(6px, 6px);
  }

  .header__mobile-toggle.active .header__mobile-toggle-line:nth-child(2) {
    opacity: 0;
  }

  .header__mobile-toggle.active .header__mobile-toggle-line:nth-child(3) {
    transform: rotate(-45deg) translate(6px, -6px);
  }

  /* Mobile Menu */
  .header__mobile-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    background: #FFFFFD;
    z-index: var(--z-modal);
    transform: translateX(-100%);
    transition: transform 0.3s ease;
    overflow-y: auto;
  }

  .header__mobile-menu.active {
    transform: translateX(0);
  }

  .header__mobile-menu-content {
    padding: 0;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .header__mobile-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 24px;
    border-bottom: 1px solid #f4f4f4;
    background: linear-gradient(135deg, #F8FBFF 0%, #EBF4FF 100%);
  }

  .header__mobile-logo-text {
    font-family: 'Inter'
    , sans-serif;
    font-size: 20px;
    font-weight: 700;
    color: #4A90E2;
  }

  .header__mobile-close {
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 8px;
    border-radius: 8px;
    transition: background-color 0.2s ease;
    color: #666;
  }

  .header__mobile-close:hover {
    background-color: rgba(74, 144, 226, 0.1);
    color: #4A90E2;
  }

  .header__mobile-close-icon {
    width: 20px;
    height: 20px;
  }

  .header__mobile-nav {
    flex: 1;
    padding: 24px;
  }

  .header__mobile-nav-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .header__mobile-nav-item {
    border-bottom: 1px solid #f4f4f4;
  }

  .header__mobile-nav-link {
    color: #222;
    text-decoration: none;
    font-size: 16px;
    font-weight: 500;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 0;
    width: 100%;
    background: transparent;
    border: none;
    cursor: pointer;
    transition: color 0.2s ease;
  }

  .header__mobile-nav-link:hover {
    color: #4A90E2;
  }

  .header__mobile-nav-arrow {
    width: 16px;
    height: 16px;
    transition: transform 0.3s ease;
  }

  .header__mobile-nav-item--has-dropdown.active .header__mobile-nav-arrow {
    transform: rotate(180deg);
  }

  .header__mobile-dropdown {
    list-style: none;
    padding-left: 20px;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .header__mobile-nav-item--has-dropdown.active .header__mobile-dropdown {
    max-height: 300px;
  }

  .header__mobile-dropdown li {
    border-bottom: 1px solid #f8f8f8;
  }

  .header__mobile-dropdown a {
    color: #666;
    text-decoration: none;
    font-size: 14px;
    padding: 12px 0;
    display: block;
    transition: color 0.2s ease;
  }

  .header__mobile-dropdown a:hover {
    color: #4A90E2;
  }

  .header__mobile-actions {
    padding: 24px;
    border-top: 1px solid #f4f4f4;
    background: linear-gradient(135deg, #F8FBFF 0%, #EBF4FF 100%);
  }

  .header__mobile-support {
    display: flex;
    align-items: center;
    gap: 12px;
    background: white;
    padding: 16px;
    border-radius: 12px;
    border: 1px solid rgba(74, 144, 226, 0.2);
    box-shadow: 0 2px 8px rgba(74, 144, 226, 0.1);
  }

  .header__mobile-support-icon {
    width: 20px;
    height: 20px;
    color: #4A90E2;
  }

  .header__mobile-support-content {
    flex: 1;
  }

  .header__mobile-support-label {
    font-size: 12px;
    font-weight: 600;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .header__mobile-support-btn {
    background: linear-gradient(135deg, #4A90E2 0%, #87CEEB 100%);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 6px 12px;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 4px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: 0 2px 6px rgba(74, 144, 226, 0.3);
  }

  .header__mobile-support-btn:hover {
    background: linear-gradient(135deg, #3A7BD5 0%, #6BB6D6 100%);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(74, 144, 226, 0.4);
  }

  /* Responsive Design */
  @media (max-width: 1200px) {
    .header__nav-list {
      gap: 16px;
      font-size: 14px;
    }


    /* Mega Menu Responsive */
    .header__nav-mega-dropdown {
      width: 800px;
      padding: 32px;
    }

    .header__nav-mega-content {
      grid-template-columns: 1fr 1fr 1fr 240px;
      gap: 32px;
    }
  }

  @media (max-width: 1024px) {
    .header__nav-list {
      gap: 12px;
      font-size: 13px;
      flex-wrap: wrap;
      justify-content: center;
    }

    .header__nav-link {
      padding: 6px 8px;
    }

    .header__nav-sale-badge {
      font-size: 9px;
      padding: 1px 4px;
    }

    /* Mega Menu Responsive */
    .header__nav-mega-dropdown {
      width: 700px;
      padding: 24px;
    }

    .header__nav-mega-content {
      grid-template-columns: 1fr 1fr 200px;
      gap: 24px;
    }
  }

  @media (max-width: 768px) {
    .header__content {
      grid-template-columns: auto 1fr auto;
      gap: 16px;
      padding: 16px 0;
    }

    .header__tagline {
      font-size: 12px;
      padding: 0 12px;
    }

    .header__support {
      display: none;
    }

    .header__logo {
      justify-self: start;
    }

    .header__logo-text {
      font-size: 24px;
      padding: 10px 16px;
    }

    .header__actions {
      gap: 12px;
    }

    .header__action-btn {
      width: 40px;
      height: 40px;
    }

    .header__mobile-toggle {
      display: flex;
    }

    .header__nav {
      display: none;
    }

    /* Hide mega menu on mobile */
    .header__nav-mega-dropdown {
      display: none;
    }

    .announcement-bar__content {
      flex-direction: column;
      gap: 8px;
      text-align: center;
      padding: 0 24px;
    }

    .announcement-bar__text {
      font-size: 13px;
    }

    .announcement-bar__close {
      right: 8px;
      width: 28px;
      height: 28px;
      font-size: 16px;
    }

    /* Mobile menu improvements */
    .header__mobile-menu {
      width: 100%;
      max-width: 100%;
    }

    .header__mobile-nav-link {
      font-size: 18px;
      padding: 18px 0;
    }

    .header__mobile-dropdown a {
      font-size: 15px;
      padding: 14px 0;
    }
  }

  @media (max-width: 480px) {
    .header__logo-text {
      font-size: 20px;
      padding: 8px 12px;
    }

    .header__actions {
      gap: 8px;
    }

    .header__action-btn {
      width: 36px;
      height: 36px;
    }

    .header__action-icon {
      font-size: 18px;
    }

    .header__cart-count {
      font-size: 10px;
      min-width: 18px;
      height: 18px;
      top: -6px;
      right: -6px;
    }

    .header__mobile-header {
      padding: 16px 20px;
    }

    .header__mobile-nav {
      padding: 20px;
    }

    .header__mobile-actions {
      padding: 20px;
    }
  }

  /* Claim Modal Styles */
  .claim-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .claim-modal.active {
    opacity: 1;
    visibility: visible;
  }

  .claim-modal__overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    cursor: pointer;
  }

  .claim-modal__content {
    background: #FFFFFD;
    border-radius: 16px;
    padding: 32px;
    max-width: 500px;
    width: 90%;
    max-height: 90vh;
    overflow-y: auto;
    position: relative;
    transform: translateY(20px);
    transition: transform 0.3s ease;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .claim-modal.active .claim-modal__content {
    transform: translateY(0);
  }

  .claim-modal__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
    padding-bottom: 16px;
    border-bottom: 2px solid rgba(74, 144, 226, 0.1);
  }

  .claim-modal__title {
    font-size: 24px;
    font-weight: 700;
    color: #222;
    margin: 0;
  }

  .claim-modal__close {
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 8px;
    border-radius: 8px;
    transition: background-color 0.2s ease;
    color: #666;
  }

  .claim-modal__close:hover {
    background-color: rgba(74, 144, 226, 0.1);
    color: #4A90E2;
  }

  .claim-modal__close-icon {
    width: 20px;
    height: 20px;
  }

  /* Claim Form Styles */
  .claim-form {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .claim-form__field {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .claim-form__label {
    font-size: 14px;
    font-weight: 600;
    color: #222;
  }

  .claim-form__input,
  .claim-form__select,
  .claim-form__textarea {
    padding: 12px 16px;
    border: 2px solid #f4f4f4;
    border-radius: 12px;
    font-size: 16px;
    transition: all 0.3s ease;
    background: #FFFFFD;
  }

  .claim-form__input:focus,
  .claim-form__select:focus,
  .claim-form__textarea:focus {
    outline: none;
    border-color: #4A90E2;
    box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.1);
  }

  .claim-form__textarea {
    resize: vertical;
    min-height: 100px;
  }

  .claim-form__actions {
    display: flex;
    gap: 12px;
    justify-content: flex-end;
    margin-top: 8px;
  }

  .claim-form__btn {
    padding: 12px 24px;
    border: none;
    border-radius: 12px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .claim-form__btn--secondary {
    background: #f4f4f4;
    color: #666;
  }

  .claim-form__btn--secondary:hover {
    background: #e0e0e0;
    color: #222;
  }

  .claim-form__btn--primary {
    background: linear-gradient(135deg, #4A90E2 0%, #87CEEB 100%);
    color: white;
    box-shadow: 0 2px 6px rgba(74, 144, 226, 0.3);
  }

  .claim-form__btn--primary:hover {
    background: linear-gradient(135deg, #3A7BD5 0%, #6BB6D6 100%);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(74, 144, 226, 0.4);
  }

  .claim-form__btn--primary:disabled {
    opacity: 0.6;
    cursor: not-allowed;
    transform: none;
  }

  .claim-form__loading-icon {
    width: 16px;
    height: 16px;
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  /* Success/Error Messages */
  .claim-form__message {
    padding: 12px 16px;
    border-radius: 8px;
    margin-bottom: 16px;
    font-weight: 500;
  }

  .claim-form__message--success {
    background: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
  }

  .claim-form__message--error {
    background: #f8d7da;
    color: #721c24;
    border: 1px solid #f5c6cb;
  }

  /* Responsive Modal */
  @media (max-width: 768px) {
    .claim-modal__content {
      padding: 24px;
      margin: 16px;
      width: calc(100% - 32px);
    }

    .claim-modal__title {
      font-size: 20px;
    }

    .claim-form__actions {
      flex-direction: column;
    }

    .claim-form__btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>